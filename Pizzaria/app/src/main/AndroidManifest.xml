<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.diegoherzer.pizzaria">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".loginActivity" />
        <activity android:name=".funcionarioActivity" />
        <activity android:name=".activities.ProdutosActivity323" />
        <activity android:name=".BebidaActivity" />
        <activity android:name=".activities.VendaActivity" />
        <activity
            android:name=".activities.Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.ProdutosCadastrarActivity" />
        <activity android:name=".activities.ProdutosModificarActivity" />
        <activity android:name=".activities.ProdutosExcluirActivity" />
        <activity android:name=".activities.ProdutosListarActivity" />
        <activity android:name=".activities.VendaIniciarActivity" />
        <activity android:name=".activities.VendaListarActivity" />
        <activity android:name=".activities.ProdutosActivity"></activity>
    </application>

</manifest>